# 🚀 启动脚本总览

## 📊 脚本架构图

```
parking-ai-customer-service/
│
├── start-all.sh          ⭐ 一键启动所有服务
│   ├── 清理端口 (3000, 5173, 5174)
│   ├── 检查依赖 (Node.js, npm, node_modules)
│   ├── 验证环境配置 (server/.env)
│   ├── 启动后端服务 → logs/backend.log
│   ├── 启动用户端   → logs/user-client.log
│   └── 启动客服端   → logs/agent-client.log
│
├── stop-all.sh           🛑 停止所有服务
│   ├── 读取 PID 文件并停止进程
│   ├── 强制清理端口占用
│   └── 可选清理日志文件
│
├── start-backend.sh      ⚡ 快速启动后端
│   ├── 清理端口 3000
│   └── 启动后端服务（前台运行）
│
└── SCRIPTS.md            📖 详细使用文档
```

## 🎯 使用场景

| 场景 | 推荐脚本 | 说明 |
|------|---------|------|
| **日常开发（全栈）** | `./start-all.sh` | 同时启动前后端，可在浏览器中测试完整功能 |
| **后端开发** | `./start-backend.sh` | 只需要测试 API，不需要前端界面 |
| **停止服务** | `./stop-all.sh` | 一键停止所有服务，清理资源 |
| **快速重启** | `./stop-all.sh && ./start-all.sh` | 完全重启所有服务 |

## ⚙️ 端口分配

| 服务 | 端口 | URL |
|------|------|-----|
| 后端 API | 3000 | http://localhost:3000 |
| 用户端 | 5173 | http://localhost:5173 |
| 客服端 | 5174 | http://localhost:5174 |

## 📝 日志文件

所有服务的输出都会保存到 `logs/` 目录：

```
logs/
├── backend.log       # 后端服务日志
├── user-client.log   # 用户端日志
└── agent-client.log  # 客服端日志
```

**实时查看日志**：
```bash
# 查看后端日志
tail -f logs/backend.log

# 查看所有日志
tail -f logs/*.log
```

## 🔧 PID 文件

脚本运行时会创建 PID 文件用于进程管理：

```
.backend.pid       # 后端服务进程 ID
.user-client.pid   # 用户端进程 ID
.agent-client.pid  # 客服端进程 ID
```

这些文件在停止服务时会自动删除。

## 🎨 终端输出示例

### start-all.sh 输出

```
========================================
  停车场智能客服系统 - 启动脚本
========================================

[清理] 检查并清理占用的端口...
  ✓ 端口 3000 空闲
  ✓ 端口 5173 空闲
  ✓ 端口 5174 空闲

[检查] 验证项目依赖...
  ✓ Node.js 版本: v18.17.0
  ✓ npm 版本: 9.6.7
  ✓ 所有依赖已就绪

[检查] 验证环境配置...
  ✓ 环境配置文件存在

========================================
  开始启动服务...
========================================

[1/3] 启动后端服务 (端口 3000)...
  ✓ 后端服务已启动 (PID: 12345)
      日志: logs/backend.log

[2/3] 启动用户端 (端口 5173)...
  ✓ 用户端已启动 (PID: 12346)
      日志: logs/user-client.log

[3/3] 启动客服端 (端口 5174)...
  ✓ 客服端已启动 (PID: 12347)
      日志: logs/agent-client.log

========================================
  ✓ 所有服务启动成功！
========================================

访问地址：
  用户端： http://localhost:5173
  客服端： http://localhost:5174
  后端API： http://localhost:3000

按 Ctrl+C 停止所有服务
========================================
```

## 💡 高级用法

### 1. 后台运行（不占用终端）

```bash
# 使用 nohup 在后台运行
nohup ./start-all.sh > /dev/null 2>&1 &

# 查看运行状态
ps aux | grep -E 'node|vite'

# 停止服务
./stop-all.sh
```

### 2. 启动时自动打开浏览器

在脚本末尾添加：
```bash
# macOS
open http://localhost:5173

# Linux
xdg-open http://localhost:5173
```

### 3. 只启动特定服务

修改 `start-all.sh`，注释掉不需要的服务：

```bash
# 只启动后端和用户端，不启动客服端
# 注释掉客服端启动部分
```

## ⚠️ 故障排查

### 问题 1: 脚本没有权限

**症状**：
```
-bash: ./start-all.sh: Permission denied
```

**解决**：
```bash
chmod +x start-all.sh stop-all.sh start-backend.sh
```

### 问题 2: 端口被占用

**症状**：
```
Port 5173 is in use, trying another one...
```

**解决**：
- 脚本会自动清理端口
- 如果仍有问题，手动清理：
  ```bash
  lsof -ti:3000,5173,5174 | xargs kill -9
  ```

### 问题 3: 环境配置未找到

**症状**：
```
✗ 未找到 server/.env 文件
```

**解决**：
```bash
cd server
cp .env.example .env
# 编辑 .env 文件，填入阿里云密钥
```

### 问题 4: 依赖安装失败

**症状**：
```
npm ERR! code ENOENT
```

**解决**：
```bash
# 清理并重新安装
rm -rf node_modules package-lock.json
npm install
```

## 🔗 相关文档

- [SCRIPTS.md](./SCRIPTS.md) - 详细使用说明
- [README.md](./README.md) - 项目文档
- [CLAUDE.md](./CLAUDE.md) - 项目架构说明

---

**快速开始命令**：
```bash
./start-all.sh     # 启动所有服务
./stop-all.sh      # 停止所有服务
```

**Happy Coding! 🎉**
